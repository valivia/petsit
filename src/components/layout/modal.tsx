import { PropsWithChildren } from "react";
import Modal from "react-modal";
interface Props {
}

Modal.setAppElement('#__next');

export const Section = ({ children }: PropsWithChildren<Props>) => {

  return (

  );
}
